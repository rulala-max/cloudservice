A . 바탕화면에 클라우드서비스폴더생성>임의파일생성>git init>git add.
>commit -m 
> git config --global user.email "rulala212@gmail.com"
> git config --global user.name "rulala"
>git branch -M main
> git remote add origin https://github.com/rulala-max/cloudservice.git
>git remote -v
>git pull origin main --rebase(원격내용 받아서 합치기)
> git push -u origin main
 



git clone 원격리포지토리주소 > 현재폴더에서 폴더하나 더 생성해서 그 안에 파일들 가져옴
git clone 원격리포지토리주소 . >현재폴더에 바로 파일들 가져옴
git config --list


B. 바탕화면 폴더 삭제
git clone https://github.com/rulala-max/cloudservice.git
cd cloudservice
(파일 추가)
git add .
git commit -m "init"
git push
------------------
리눅스 명령어
vi에디터 화면이 나오면 j(아래), k(위), q(편집화면 탈출)


2교시
다른 사람의 원격리포지토리를 클론 후 새파일 추가 또는 기존파일 편집 후 로컬리포지토리에 커밋

원 저작자와 바인딩된 원격리포지토리 주소를 내 원격리포지토리 주소로 업데이트

확인 git remote -v
삭제 git remote remove origin
등록 git remote add origin 내가만든원격리포지토리주소
확인 git remote -v

업로드 git push
오류발생? (새로 만든 깃 리모트 리포지토리 주소와 바인딩 필요)
업로드 git push -u origin main