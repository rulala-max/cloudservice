fork : 다른사람것 원격에서 내 깃허브원격으로 퍼오기
>내 로컬에 클론>새파일만들고 기존파일에 코드추가했을경우>깃에드후 (* git reset하면 스테이지에 올린거 다시 내리기)
>커밋메세지, 푸시>내 원격에만 수정내용 반영됨
>pull request (기여요청) >상대가 머지 수락>합쳐지면서 원저작자의 컨트리뷰트에 내 것 들어감/상대의 인사이트의 네트워크에서도 확인가능

충돌상황(로컬에서 새 파일 만들고 로컬에서 기존파일 수정)
>에드,커밋후 원격에서도 07변경 후 push > 리젝트됨
>git fetch>푸시>거부(원격에 로컬에없는 파일이 있다)>git pull 오류(로컬 원격에서 각각 같은 파일 수정해서)>깃 에드 후커밋 다시

 git log --oneline --graph
-git commit --amend -m "~" : 커밋메세지 내용바꾸기
-탭 : 자동완성  

-a
